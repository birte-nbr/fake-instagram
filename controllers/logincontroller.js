// tell ejs template what to display

/*
showLogin

async checkLogin(req, res){
    // check if verifyUser is empty or not 
    // if not, use name, email, id and put it in session
    
    let user = [];
    try {
        user.verifyUser(req.body)
        //return res.redirect(302, "/") // redirect user to the page they came from 
    }
    catch {

    }
}

*/