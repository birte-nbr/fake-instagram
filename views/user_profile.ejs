<!DOCTYPE html>
<html lang="en">


<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css" />

    <style>
        .code-block {
            background-color: #f5f5f5; /* Background color */
            padding: 10px; /* Padding around the code block */
            border-radius: 5px; /* Rounded corners */
        }
        pre {
            margin: 0; /* Remove default margin */
        }
        code {
            font-family: 'Courier New', Courier, monospace; /* Specify a monospaced font */
            color: #333; /* Text color */
        }
    </style>
</head>
<body>
    <h1>User Details</h1>
    

        <p>My Username:
            <%= user.username %>
        </p>


        <p>My Name:    <%= user.given_name%>   <%=user.surname %>
        </p>

        <p> I'm from: 
            <%= user.country %>
        </p>
        
  <% if (profilePosts.length > 0) { %>
        <% for( let i = 0; i < profilePosts.length; i++) { %> 
            <img 
            src="/assets/uploads/<%= profilePosts[i].path%>" 
            alt="<%= profilePosts[i].alt_text%>" />

        <%= profilePosts[i].caption  %>
   

        <% } %>
 
 
   <% } else { %>
        <p>no photos yet!</p>
    <% } %>
        <% if (user.username == '') { %>        <p>No user found</p>
            <% } %>
            
         
<a href="/users">Back</a>

</body>
</html>
